package decorator;

public class Star extends TreeDecorator
{
    private Tree tree;

    public Star(Tree tree)
    {
        //super(FileReader.getLines("decorator/txt/star.txt"));
        //this.tree = tree;
        //super(tree.lines);
        //super.integrateDecor(FileReader.getLines("decorator/txt/star.txt"));

        //super(tree);
        //tree.integrateDecor(FileReader.getLines("decorator/txt/star.txt"));
        
    }
}
